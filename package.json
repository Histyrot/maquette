{
  "name": "taty-vanille",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i ./src/input.css -o ./assets/css/styles.css -m",
    "build:js": "esbuild assets/js/site.js --minify --outfile=assets/js/site.min.js --target=es2018",
    "build": "npm run build:css && npm run build:js"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "tailwindcss": "^4.1.0"
  }
}
